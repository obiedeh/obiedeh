{
  "profile": "jetson",
  "adapter": "cosmos-reason2",
  "adapter_endpoint": "http://127.0.0.1:8000/v1",
  "model": "nvidia/cosmos-reason2-2b",
  "api_key_env": "COSMOS_API_KEY",
  "notes": "Use Cosmos-Reason2 through the dedicated adapter built on NVIDIA NIM's OpenAI-compatible Chat Completions API. Keep rules, evidence, runtime telemetry, and API ingestion model-agnostic."
}
